<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE hibernate-configuration
        PUBLIC "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>

    <session-factory>

    <!--Driver class for DB connection-->
    <property name="hibernate.connection.driver_class">
        org.h2.Driver
    </property>

    <!--URL for connection to DB-->
    <property name="hibernate.connection.url">
        jdbc:h2:file:C:/programming/thaibooking/db
    </property>

    <!--DB user name-->
    <property name="hibernate.connection.username">
        sa
    </property>

    <!--DB user password-->
    <property name="hibernate.connection.password">
    </property>

    <!--Hibernate dialect for building queries-->
    <property name="hibernate.dialect">
        org.hibernate.dialect.H2Dialect
    </property>

    <!--With update,the database schema will be updated when the SessionFactory is closed explicitly-->
    <property name="hibernate.hbm2ddl.auto">
        create
    </property>

    <!--Show queries in the console-->
    <property name="hibernate.show_sql">
        true
    </property>

    <property name="hibernate.connection.pool_size">
        1
    </property>

    </session-factory>
</hibernate-configuration>